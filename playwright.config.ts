import { defineConfig, devices } from '@playwright/test';
export default defineConfig({testDir:'./tests/e2e',reporter:[['html',{open:'never'}],['list']],webServer:{command:'pnpm build && pnpm --filter @eazo/life-elsewhere-now exec vite preview --host 127.0.0.1 --port 5103',port:5103,reuseExistingServer:false},use:{baseURL:'http://127.0.0.1:5103',trace:'retain-on-failure',screenshot:'only-on-failure'},projects:[{name:'pixel',use:{...devices['Pixel 7']}},{name:'iphone',use:{...devices['iPhone 12']}}]});
